# Configuration

The `template.yaml` file defines the entire cluster and plugin configuration. It is generated by the [`init`](commands.md#init) command and can be customized manually.

## Structure

```yaml
name: my-cluster              # Cluster name
provider:
  type: kind                  # Kubernetes provider
cluster:
  controlPlanes: 1            # Number of control planes
  workers: 2                  # Number of workers
  version: v1.31.0            # Kubernetes version
plugins:
  storage: { ... }            # StorageClass provisioner
  ingress: { ... }            # Ingress controller
  certManager: { ... }        # TLS certificate management
  externalDNS: { ... }        # Automatic DNS management
  istio: { ... }              # Service mesh
  monitoring: { ... }         # Prometheus + Grafana
  dashboard: { ... }          # Kubernetes dashboard
  customApps: [ ... ]         # Custom Helm charts
  argocd: { ... }             # GitOps with ArgoCD
```

## Base Fields

| Field | Type | Default | Required | Description |
|-------|------|---------|:---:|-------------|
| `name` | string | `my-cluster` | yes | Cluster name (used as kind name) |
| `provider.type` | string | - | yes | Kubernetes provider. Values: `kind` |
| `cluster.controlPlanes` | int | `1` | yes | Number of control plane nodes (min: 1) |
| `cluster.workers` | int | `2` | yes | Number of worker nodes (min: 0) |
| `cluster.version` | string | - | no | Kubernetes version (e.g., `v1.31.0`) |

## Plugins

Each plugin has an `enabled: true/false` field. Plugins not present in the YAML are considered disabled.

Detailed documentation for each plugin can be found in the [plugins/](plugins/) folder:

| Plugin | Documentation | Description |
|--------|--------------|-------------|
| Storage | [plugins/storage.md](plugins/storage.md) | StorageClass provisioner |
| Ingress | [plugins/ingress.md](plugins/ingress.md) | NGINX ingress controller |
| Cert-Manager | [plugins/cert-manager.md](plugins/cert-manager.md) | TLS certificate management |
| External DNS | [plugins/external-dns.md](plugins/external-dns.md) | Automatic DNS management |
| Istio | [plugins/istio.md](plugins/istio.md) | Service mesh with mTLS |
| Monitoring | [plugins/monitoring.md](plugins/monitoring.md) | Prometheus + Grafana via Helm |
| Dashboard | [plugins/dashboard.md](plugins/dashboard.md) | Headlamp via Helm |
| Custom Apps | [plugins/custom-apps.md](plugins/custom-apps.md) | Custom Helm charts |
| ArgoCD | [plugins/argocd.md](plugins/argocd.md) | GitOps with ArgoCD |

## Environment Variables

The `--env` flag (default: `.env`) loads environment variables from a file. Useful for secrets (e.g., SSH keys for ArgoCD).

`.env` file format:

```bash
ARGOCD_SSH_KEY="-----BEGIN OPENSSH PRIVATE KEY-----
...
-----END OPENSSH PRIVATE KEY-----"
```

Reference in template:

```yaml
argocd:
  repos:
    - name: private-repo
      url: git@github.com:user/repo.git
      sshKeyEnv: ARGOCD_SSH_KEY
```

## Templating

klastr supports Go templates in `template.yaml` for dynamic configuration:

```yaml
name: "{{ env "CLUSTER_NAME" "my-cluster" }}"
cluster:
  workers: {{ env "WORKERS" "2" }}
plugins:
  argocd:
    apps:
      - name: my-app
        values:
          image:
            tag: "{{ .GitCommit }}"
```

See [Templating Guide](templating.md) for complete documentation.

## Complete Example

```yaml
name: my-cluster
provider:
  type: kind
cluster:
  controlPlanes: 1
  workers: 2
  version: v1.31.0
plugins:
  storage:
    enabled: true
    type: local-path
  ingress:
    enabled: true
    type: nginx
  certManager:
    enabled: true
    version: v1.16.3
  externalDNS:
    enabled: true
    provider: cloudflare
    zone: example.com
    source: ingress
    credentials:
      apiToken: ${CF_API_TOKEN}
  istio:
    enabled: true
    profile: default
    ingressGateway: true
  monitoring:
    enabled: true
    type: prometheus
    ingress:
      enabled: true
      host: grafana.localhost
  dashboard:
    enabled: true
    type: headlamp
    ingress:
      enabled: true
      host: headlamp.localhost
  customApps:
    - name: redis
      chartName: redis
      chartRepo: https://charts.bitnami.com/bitnami
      version: "21.1.5"
      namespace: redis
      values:
        architecture: standalone
  argocd:
    enabled: true
    namespace: argocd
    version: stable
    ingress:
      enabled: true
      host: argocd.localhost
    repos:
      - name: my-gitops-repo
        url: git@github.com:user/gitops-repo.git
        type: git
        sshKeyFile: ~/.ssh/id_ed25519
    apps:
      - name: nginx
        namespace: demo-app
        repoURL: https://charts.bitnami.com/bitnami
        chart: nginx
        targetRevision: 18.2.4
        values:
          replicaCount: 2
```
