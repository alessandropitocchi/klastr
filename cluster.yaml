name: my-cluster
provider:
    type: kind
cluster:
    controlPlanes: 1
    workers: 2
    version: v1.31.0
plugins:
    argocd:
        enabled: true
        namespace: argocd
        version: stable
        repos:
            - name: my-gitops-repo
              url: git@git.elmec.com:PITOCCHI/test-argo.git
              type: git
              sshKeyFile: ~/.ssh/argocd_gitlab_ed25519